{
  "framework": "nextjs",
  "buildCommand": "npm run payload -- migrate && npm run build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/send-campaigns",
      "schedule": "0 8 * * *"
    }
  ]
}
