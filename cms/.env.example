# ============================================================================
# GINGERCO CMS - ENVIRONMENT VARIABLES
# ============================================================================
# IMPORTANT: Never commit .env.local to Git
# Only .env.example should be in version control
#
# For LOCAL DEVELOPMENT:
# - Copy this file to .env.local
# - Fill in your Supabase credentials
# - .env.local is gitignored and won't be committed
#
# For PRODUCTION (Vercel):
# - Set these variables in Vercel under Settings > Environment Variables
# - Never commit secrets to the repository
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
# Get these from: Supabase Console > Settings > API > Project Settings
#
# NEXT_PUBLIC_SUPABASE_URL: Your Supabase project URL
#   Example: https://abcdefghijklmnop.supabase.co
#
# NEXT_PUBLIC_SUPABASE_ANON_KEY: Supabase anonymous public key (used client-side)
#   Available in: Supabase Console > Settings > API > Project API Keys
#   Look for: "anon public" key
#
# SUPABASE_SERVICE_ROLE_KEY: Supabase service role key (used server-side only)
#   Available in: Supabase Console > Settings > API > Project API Keys
#   Look for: "service_role" key
#   ⚠️  NEVER expose this in client-side code or public repositories

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# NEXT_PUBLIC_SITE_URL: Public URL of your application
#   Local: http://localhost:3000
#   Staging: https://cms-staging.gingerandco.at
#   Production: https://cms.gingerandco.at
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# NODE_ENV: Environment type
#   - development: Local development with full logging
#   - production: Optimized for production, minimal logging
NODE_ENV=development

# ============================================================================
# OPTIONAL: EXTERNAL SERVICES (For Future Phases)
# ============================================================================

# RESEND_API_KEY: Email service API key (for sending confirmation emails)
#   Get from: https://resend.com/api-keys
#   Note: Set this in Vercel, NOT in .env.local for security
# RESEND_API_KEY=your-resend-api-key-here

# STRIPE_PUBLIC_KEY: Stripe public key (for payments)
#   Get from: Stripe Dashboard > Developers > API Keys
# STRIPE_PUBLIC_KEY=pk_test_...

# STRIPE_SECRET_KEY: Stripe secret key (server-side only)
#   ⚠️  NEVER expose this in client-side code
# STRIPE_SECRET_KEY=sk_test_...

# ============================================================================
# OPTIONAL: ENCRYPTION KEYS (For Sensitive Data)
# ============================================================================
# These are used to encrypt personal information (email, phone, address)
# in the database. These should be strong, random strings.
#
# Generate with: openssl rand -hex 32
#
# Note: Encryption/decryption happens in Next.js application layer,
# not at database level. Keep these keys secure and never commit them.

# ENCRYPTION_KEY=your-64-character-hex-string-here
# HMAC_SECRET=your-64-character-hex-string-here

# ============================================================================
# INSTRUCTIONS FOR SETUP
# ============================================================================
#
# LOCAL DEVELOPMENT:
# ─────────────────
# 1. Copy this file to .env.local
#    $ cp .env.example .env.local
#
# 2. Get your Supabase credentials:
#    - Go to https://supabase.com
#    - Open your "gingerco-cms" project
#    - Settings > API > Project Settings
#    - Copy the URL and keys
#
# 3. Fill in .env.local:
#    - NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
#    - SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
#
# 4. Start development:
#    $ npm run dev
#
# 5. Open browser: http://localhost:3000
#
#
# PRODUCTION (Vercel):
# ────────────────────
# 1. DO NOT commit .env.local to Git
#    (It's already in .gitignore)
#
# 2. Set environment variables in Vercel:
#    a. Go to: Settings > Environment Variables
#    b. Add each variable from .env.example
#    c. Click: Save
#
# 3. Vercel will automatically use these during build and at runtime
#
# 4. Your secrets are:
#    - Protected (not in Git)
#    - Encrypted at rest in Vercel
#    - Rotatable (update and redeploy)
#
#
# SECURITY BEST PRACTICES:
# ────────────────────────
# ✓ Never commit .env.local
# ✓ Use .env.example to document structure
# ✓ Rotate secrets regularly
# ✓ Use different credentials for dev/staging/production
# ✓ Restrict who can access Vercel environment settings
# ✓ Use strong, random keys for encryption
# ✓ Keep SUPABASE_SERVICE_ROLE_KEY off client-side
# ✓ Monitor audit logs for suspicious activity
#
# ============================================================================
